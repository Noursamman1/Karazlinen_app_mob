# Agent 1 Implementation Plan

- Establish canonical source of truth under `docs/`, `contracts/`, and `adr/`
- Normalize public API semantics around the BFF, not Magento
- Document auth, error, and cache rules for downstream app and BFF consumers
