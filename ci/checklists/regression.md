# Regression Checklist

- App boots in Arabic by default
- RTL layout is preserved across catalog and account flows
- Catalog listing, search, and product details render sane fallback states
- Protected account and order routes respect session state
- BFF health endpoints and auth endpoints respond with normalized errors
