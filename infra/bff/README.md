# BFF Local Infra

- `docker compose up --build` starts the NestJS BFF and Redis.
- Use environment variables from `bff/.env.example` as a local bootstrap only.
- Replace keys and Magento URLs before using beyond local development.
